# Model weights live here but are never committed — they're large binaries
# managed by `modelctl`, not source.
/models/

# Python
__pycache__/
*.pyc

# Scaffold cruft, not part of modelctl
.DS_Store
.python-version
/local-llm-stack/
/main.py
/pyproject.toml
