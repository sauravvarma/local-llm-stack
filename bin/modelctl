#!/usr/bin/env bash
# Thin launcher so `modelctl` works from anywhere without installing the package.
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec env PYTHONPATH="$DIR${PYTHONPATH:+:$PYTHONPATH}" python3 -m modelctl "$@"
